# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtualenv
venv/
.venv/
env/
.env

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Datasets (pull via script, do not commit) — anchored to repo root
/data/
datasets/
mvtec_anomaly_detection/
*.tar
*.tar.xz
*.zip

# Model weights & engines (large binaries, not portable)
*.pt
*.pth
*.onnx
*.engine
*.plan
*.trt
/models/
/checkpoints/

# TRT engine output dir (large)
/engines/*.engine
/engines/*.plan

# but DO commit calibration cache (small, reproducibility)
!/engines/*.cache

# Build / TensorRT artifacts
build/
dist/
trt_logs/
calibration_cache/

# Logs & outputs
logs/
runs/
wandb/
mlruns/
outputs/
*.log

# DeepStream temp files
*.mp4.tmp
gst-reaper-*

# Media output (commit final demo only, in docs/)
*.mp4
*.mov
*.avi
*.ogv
!docs/demo*.mp4
!docs/deepstream_demo.mp4

# DeepStream skill artifacts — commit code/config but skip large run outputs
src/deepstream/models/*/model/*.onnx
src/deepstream/models/*/model/*.cache
src/deepstream/models/*/benchmarks/engines/
src/deepstream/models/*/benchmarks/**/timing.cache
src/deepstream/models/*/samples/*.mp4
src/deepstream/models/*/samples/*.ogv
src/deepstream/models/*/sources/test_images/*.mp4
src/deepstream/models/*/sources/test_images/*.ogv
src/deepstream/models/*/parser/*.so
src/deepstream/models/*/parser/*.o

# Secrets
*.pem
*.key
.env.local
